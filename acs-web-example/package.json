{
  "name": "acs-web-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "bun run build && bunx wrangler pages deploy dist --project-name acs-viewer --branch main --commit-dirty=true"
  },
  "dependencies": {
    "acs-web": "file:../acs-web/pkg"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^7.2.4",
    "vite-plugin-wasm": "^3.4.1",
    "vite-plugin-top-level-await": "^1.5.0"
  }
}
